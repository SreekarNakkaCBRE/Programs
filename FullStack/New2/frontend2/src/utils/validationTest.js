// Test file to verify validation functions
import { useFormValidation, validateForm, getPasswordStrength } from './validation';

// Simple test to see if functions are available
console.log('useFormValidation:', typeof useFormValidation);
console.log('validateForm:', typeof validateForm);
console.log('getPasswordStrength:', typeof getPasswordStrength);

export { useFormValidation, validateForm, getPasswordStrength };
